fastapi>=0.110.0
uvicorn[standard]>=0.27.0
openai>=1.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0
slowapi>=0.1.9            # Rate limiting par IP (ex: 10 req/min) — renvoie 429 si dépassé
python-jose[cryptography]>=3.3.0  # Création et vérification des JWT (tokens d'auth)
bcrypt>=4.0.1             # Hachage des mots de passe (irréversible, résistant brute-force)
python-multipart>=0.0.9   # Lecture des formulaires HTML (nécessaire pour /auth/token)
httpx>=0.27.0
